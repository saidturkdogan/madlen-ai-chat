# =============================================================================
# Database Configuration
# =============================================================================
# The connection string for the PostgreSQL database.
# Format: postgresql://<username>:<password>@<hostname>:<port>/<database_name>
# 
# If running locally with Docker Compose, the host might be 'localhost'.
# If running the backend inside Docker, the host should match the database service name (e.g., 'db').
#
# Example (Local): postgresql://postgres:postgres@localhost:5432/madlen_db
# Example (Docker): postgresql://postgres:postgres@db:5432/madlen_db
DATABASE_URL=postgresql://user:password@localhost:5432/madlen_db

# =============================================================================
# Authentication (Clerk.com)
# =============================================================================
# The Issuer URL for your Clerk application. 
# This is used to verify the JWT tokens sent by the frontend.
# You can find this in your Clerk Dashboard under Settings > API Keys > Advanced > Issuer
CLERK_ISSUER=https://example-domain.clerk.accounts.dev

# (Optional) The Audience claim for the JWT.
# Required only if you have configured custom audiences in Clerk.
# CLERK_AUDIENCE=my-audience

# =============================================================================
# AI Service (OpenRouter)
# =============================================================================
# API Key for OpenRouter.ai to execute LLM queries.
# You can obtain a key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# =============================================================================
# Telemetry & Monitoring (OpenTelemetry / Jaeger)
# =============================================================================
# The application is configured to send traces to a Jaeger instance running locally.
# Default Endpoint: http://localhost:4318/v1/traces
#
# No configuration is currently needed here as it defaults to localhost,
# but can be extended in the future.